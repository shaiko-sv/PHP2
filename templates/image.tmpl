
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Работа с файлами</title>
</head>
<body>
  <h1>{{ title }}</h1>
  <a href="/"> Вернуться в галерею </a>
  <div>
    <img src="{{ path }}{{ image.file_name }}.{{ image.ext }}">
  </div>
</body>
</html>
