<header>
    <h1>{{ title }}</h1>
</header>


<div class="images">
    {% for image in images %}
    <a href="/image?photo={{ image.id }}">
        <img src="{{ path }}{{ image.file_name }}.{{ image.ext }}">
    </a>
    {% endfor %}
</div>

<div class="add_foto">
    <form action="" method="POST" enctype="multipart/form-data">
        <span> <b>Добавить файл: </b> </span>
        <input type="file" name="userFile">
        <button type="submit" name="send">ЗАГРУЗИТЬ</button> <br>
        <span>{{ message }}</span>
    </form>
</div>